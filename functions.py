# define a function named get_user_num that will ask the user for an a number
# and if it is a valid number return the number as a float. If it is not
# a valid number than return 0 - america
# def get_user_num():
#     number=input("Give me a number\n")
#     if number.isnumeric():
#         return float(number)
#     else:
#         return 0

# define a function named convert_to_pig_latin that takes a word as a parameter and
# returns the pig latin version of that word. If an empty string is given return an empty string
# yes - esya
# hello - elloha
# a - aa
# i - ia
# "" - ""
# - freedom

# def convert_to_pig_latin(word):
#     if word == "":
#         return ""

#     first_letter = word[0:1]
#     rest_of_word = word[1:]
#     new_word = rest_of_word + first_letter + "a"
#     return new_word



# define a function named convert_from_inches that takes as a first parameter the number of inches and the second
# paramter will be the units to convert to such as "cm", "m", "ft". The default for second parameter is "cm".
# based on the units given convert inches into the correct units. - eagle

# def convert_from_inches(inch, unit="cm"):
#     if unit == "cm":
#         return 2.54 * inch
#     elif unit == "m":
#         return 0.0254 * inch
#     elif unit == "ft":
#         return 0.0833333334 * inch
        







